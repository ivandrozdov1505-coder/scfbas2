## 2023-11-20 - Adding ARIA labels to Icon Buttons
**Learning:** Icon-only buttons without ARIA labels are a common accessibility issue. They are especially problematic for screen reader users. Adding aria-labels makes the purpose clear.
**Action:** Always add aria-labels to buttons that only contain icons.
